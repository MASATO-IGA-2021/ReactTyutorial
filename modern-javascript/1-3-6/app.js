const APP_TITLE = 'Reactアプリ';

export function getTriangle(base, height) {
    return base* height / 2
};

export const getRectango= (base, height) => base* height

export class Article {
    getAppTitle() {
        return APP_TITLE;
    }
}